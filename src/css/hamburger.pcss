/**
 * Hamburger
 */

.spline,
.cross {
	--speed: 0.25s;
	--actual-speed: calc(var(--speed) / 2);

	&,
	&::before,
	&::after {
		@apply bg-black;
		border-radius: 1em;
		content: '';
		height: 0.2em;
		left: 50%;
		position: absolute;
		transform: translateX(-50%);
		transition: transform var(--actual-speed) ease-in-out, opacity 0s ease-in-out;
		width: 1.7em;
		z-index: 11;
	}
}

.spline {
	&,
	&::before,
	&::after {
		transition-delay: var(--actual-speed), var(--actual-speed);
	}

	&::before {
		transform: translate(-50%, -0.5em);
	}

	&::after {
		transform: translate(-50%, 0.5em);
	}

	.is-open & {
		opacity: 0%;

		&::before,
		&::after {
			transform: translate(-50%, 0);
		}

		&,
		&::before,
		&::after {
			transition-delay: 0s, var(--actual-speed);
		}
	}
}

.cross {
	background: transparent;
	opacity: 0%;
	transition-delay: var(--actual-speed), var(--actual-speed);

	.is-open & {
		opacity: 100%;

		&::before {
			transform: translateX(-50%) rotate(45deg);
		}

		&::after {
			transform: translateX(-50%) rotate(-45deg);
		}

		&::before,
		&::after {
			transition-delay: var(--actual-speed), var(--actual-speed);
		}
	}
}
